name: CSN Sengrep Pipeline

on :
  push:
    bracnches:
      - master
    pull_request:


jobs:
  semgrep:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/checkout@v4
      with:
        python-version: '3.x'

        - name: install semgrep
          run: pip install Semgrep

        - name: Run semgrep
          run: semgrep --config auti --error || true
          

  


  
